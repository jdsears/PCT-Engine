.env
.env.*
!.env.example
node_modules
npm-debug.log*

# OCR scratch files produced during ingestion
*.ocr.pdf

# Built Teams app package, regenerated from the script with the real bot id
pct-copilot-teams.zip

# OS noise
.DS_Store

# Curation pack workbook, generated from live data, not committed
CURATION_PACK_*.xlsx
